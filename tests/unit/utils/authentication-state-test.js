import AuthenticationState from 'dummy/utils/authentication-state';
import { module, test } from 'qunit';

module('Unit | Utility | authentication-state', function (/*hooks*/) {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = AuthenticationState.create();
    assert.ok(result);
  });
});

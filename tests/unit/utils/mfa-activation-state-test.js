import MfaActivationState from 'dummy/utils/mfa-activation-state';
import { module, test } from 'qunit';

module('Unit | Utility | mfa-activation-state', function (/*hooks*/) {

  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = MfaActivationState.create();
    assert.ok(result);
  });
});
